<% if @notifications.present? %>
  <% @notifications.each do |notification| %>
    <li>
      <div class="notifaction-new">
        <%= link_to "#{notification.redirect_path}", class: 'notify_link' do %>
         <!-- <div class="notification-list-img">
            <% if notification.image.attached? %>
              <%= image_tag notification.image.variant(resize: "60x60"), class: 'img-responsive' %>
            <% else %>
              <%= image_tag 'banner-1.png', class: 'img-responsive' %>
            <% end %>
          </div> -->
          <p class="notification-msg" data-toggle="tooltip" data-placement="top" title="notification message"><%= notification.message %></p>
          <%# <span class="notification-read"></span> %>
        <% end %>

      </div>
    </li>
  <% end %>
<% else %>
  <div class="notify_empty">
    <%= image_tag 'notification-bell.svg' %>
    <h4><%= t('common.no_new_notifications')%></h4>
  </div>
<% end %>
